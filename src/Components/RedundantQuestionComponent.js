import React from "react";

export default function EmailErrorComponent() {
    return (
        <div>
            <h1 style={{color: "red"}}>Sorry, looks like there's already a question for this mystery!</h1>
        </div>
    )
}